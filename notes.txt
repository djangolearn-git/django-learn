git config --global user.name "DJANGO LEARN" && git config --global user.email "djangolearn.git@gmail.com" && ssh-keygen -t rsa -b 4096 -C "djangolearn.git@gmail.com"
cat /root/.ssh/id_rsa.pub

ALTER USER 'root'@'localhost' IDENTIFIED BY 'root';
create database djangolearndb;
FLUSH PRIVILEGES;



WSGI.P =====>

"""
WSGI config for Django3Project project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/3.0/howto/deployment/wsgi/
"""

import os
import time
import traceback
import signal
import sys

from django.core.wsgi import get_wsgi_application
#final
sys.path.append('/var/projects/venv/django-learn')
sys.path.append('/var/projects/venv/lib/python3.6/site-packages')
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'djangolearn.settings')

try:
    application = get_wsgi_application()
except Exception:
    # Error loading applications
    if 'mod_wsgi' in sys.modules:
        traceback.print_exc()
        os.kill(os.getpid(), signal.SIGINT)
        time.sleep(2.5)